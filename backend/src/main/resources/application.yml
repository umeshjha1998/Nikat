spring:
  application:
    name: nikat-backend
  datasource:
    url: jdbc:postgresql://ep-calm-meadow-a106b6xh-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_VcTnHmJhoW06
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: false
    baseline-on-migrate: true

jwt:
  secret: ${JWT_SECRET:dfgsdfgsdfgsdfgsdfgsdfgsdfgsdfgsdfgsdfgsdfgsdfgsdfgsdfgsdfgsdfg}
  expiration: 86400000 # 1 day

resend:
  api:
    key: ${RESEND_API_KEY:}
  from:
    email: "onboarding@resend.dev"

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

cors:
  allowed-origins: ${ALLOWED_ORIGINS:http://localhost:4200}
